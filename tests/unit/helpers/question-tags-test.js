import { questionTags } from 'forum-question/helpers/question-tags';
import { module, test } from 'qunit';

module('Unit | Helper | question tags');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = questionTags([42]);
  assert.ok(result);
});
